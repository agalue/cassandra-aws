# @author agalue@opennms.com
---
- name: Decomission + Boostrap
  hosts: cassandra
  become: true
  order: sorted
  serial: 1
  tasks:

  - name: Rebuild Cassandra nodes in sequence
    loop: '{{ instances | dict2items }}'
    include_tasks: 'tasks-decommission-bootstrap.yaml'
